name: VIRACUT

on:
  workflow_dispatch:

# Autorise le workflow à lire et écrire dans le dépôt (nécessaire pour p.json)
permissions:
  contents: write
  actions: write

jobs:
  render:
    runs-on: ubuntu-latest
    steps:
      - name: 1. Récupération du code source
        uses: actions/checkout@v4

      - name: 2. Configuration de Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: 3. Installation FFmpeg, FFprobe et Polices
        run: |
          sudo apt-get update
          sudo apt-get install -y ffmpeg fonts-dejavu-core

      - name: 4. Vérification du fichier de données
        run: |
          if [ -f p.json ]; then
            echo "p.json trouvé. Préparation du rendu IA..."
          else
            echo "ERREUR : p.json est absent. Vérifiez l'envoi depuis l'App."
            exit 1
          fi

      - name: 5. Exécution du Rendu IA (Captions Anthropic)
        # On passe le token en variable d'environnement si besoin pour le script python
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: python render.py

      - name: 6. Archivage de la Vidéo Finale
        uses: actions/upload-artifact@v4
        with:
          name: video-viracut-ia
          path: output.mp4
          if-no-files-found: error
          retention-days: 7
